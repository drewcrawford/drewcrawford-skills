Determine if the project has a 'github' remote.

```
git remote -v
github  git@github.com:drewcrawford/logwise.git (fetch)
github  git@github.com:drewcrawford/logwise.git (push)
origin  gitea@gitea.example.com:myorg/myrepo.git (fetch)
origin  gitea@gitea.example.com:myorg/myrepo.git (push)
```

If it has NO github remote, report to the assigning agent that it succeeded.
If it HAS a github remote, report to the assigning agent that it succeeded AND we need to SKIP the next step and go directly to step 17.

