Consider the project's release history.

```bash
git tag --format='%(refname:short): %(creatordate)'
```

Do you think we're ready for a stable (that is, 1.0+) release?  If we have a long documented history of making minor nonbreaking changes, abort this checklist to tell the user to consider stabilizing the project.

If we're regularly making breaking changes, or the project is still young, proceed to the next step.