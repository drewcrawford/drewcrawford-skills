# cargo clippy

Run the standard cargo clippy-style command for this project.  Ensure they build with no warnings, i.e.

```bash
RUSTFLAGS="-D warnings" cargo clippy
```

If the project also targets wasm32-unknown-unknown, run clippy in that configuration as well.  For example,

```bash
cargo +nightly clippy --target=wasm32-unknown-unknown
```