# Tagging releases

Tag the release.

```
git tag v0.2.3
```

Use the version currently present in Cargo.toml.

# Pushing releases

Push the release to both origins

```bash
git push v0.2.3 origin && git push v0.2.3 github"

