# cargo check

Run the standard `cargo check` commands for this crate.  Ensure they build with no warnings, i.e.

```bash
RUSTFLAGS="-D warnings" cargo check
```

If the project targets wasm32-unknown-unknown as well, also check the wasm target.

