Determine if the project has a 'github' remote.

```
git remote -v
github  git@github.com:drewcrawford/logwise.git (fetch)
github  git@github.com:drewcrawford/logwise.git (push)
origin  gitea@gitea.mermaid-gecko.ts.net:Metropolis/logwise.git (fetch)
origin  gitea@gitea.mermaid-gecko.ts.net:Metropolis/logwise.git (push)
```

If it has NO github remote, go to step 19.
If it HAS a github remote, SKIP step 19 and go directly to step 20.

