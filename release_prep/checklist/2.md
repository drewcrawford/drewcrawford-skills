# Dependency audit

Now we need to audit the dependencies for this project.  Problems to look for include:

* Dependencies that appear to have been added accidentally, like to debug something locally
* Dependencies on a path on the user's system, which cannot be published.  Path dependencies inside a workspace are fine.

If you encounter any of these situations, stop and escalate to the user.

