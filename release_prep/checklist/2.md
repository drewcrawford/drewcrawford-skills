# Line counts

We need to ensure the project complies with line count requirements. 

```bash
./release_prep/scripts/line_count.py
```

When files are too long, we need to split them into multiple files.