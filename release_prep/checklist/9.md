We want to ensure that the README.md file has the most up to date information from src/lib.rs.  

We will need to make minor adjustments for the differences between GitHub-flavored markdown and rustdoc syntax, for example, in Rust we use [] and in Markdown we use ``, or the logo path is differnt.