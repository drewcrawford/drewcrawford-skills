# GitHub topics / tags

We need to check if the GitHub topics (tags) are good choices for the project.

```bash
gh repo view --json repositoryTopics
```

Based on your knowledge of the project, determine if these tags are still accurate or should be expanded.

Then, update the topics:

```bash
gh repo edit --add-topic topic1,topic2
gh repo edit --remove-topic old-topic
```

