Run the symlink check script:

```bash
~/.claude/skills/release_prep/scripts/11_ensure_agents_symlink
```

It verifies that AGENTS.md exists and that CLAUDE.md is a symlink to it, creating or fixing the symlink if needed. It prints whether edits were made; report that.

If the script reports that AGENTS.md does not exist, report an error.
