# check_all

Run the project's check_all script which handles format, check, clippy, docs, and tests:

```bash
./scripts/check_all
```

Note: using the syntax `./` ensures you won't have permissions issues.

If this script does not exist, abort the checklist immediately.

If any checks fail, abort the checklist.