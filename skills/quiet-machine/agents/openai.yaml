interface:
  display_name: "Quiet Machine"
  short_description: "Run durable benchmarks on a quiet Hetzner VM"
  default_prompt: "Use $quiet-machine to run this benchmark durably on an exclusive Hetzner VM, then wait for and retrieve its results."
