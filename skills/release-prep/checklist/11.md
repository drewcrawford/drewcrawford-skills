Run the symlink check script in check-only mode:

```bash
scripts/11_ensure_agents_symlink
```

If it reports drift, repair a missing or incorrect symlink explicitly:

```bash
scripts/11_ensure_agents_symlink --apply
```

If `CLAUDE.md` is a regular file, inspect it before deciding whether replacing it is appropriate. Replacement requires both `--apply` and `--force`.

If the script reports that AGENTS.md does not exist, report an error.
